# Google OAuth Client ID (type "Web application") from
# https://console.cloud.google.com/apis/credentials — add this app's origin
# (e.g. http://localhost:3000) under "Authorized JavaScript origins".
# Safe to expose to the browser: Google Identity Services requires it client-side.
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
